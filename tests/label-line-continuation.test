#!/bin/sh

# Check that ratfor do automatic line continuation after lables, until a
# statement is found.

. ../lib/rat4-t.sh || exit 99

cat > tst.r <<EOF
100
foo

200


bar
EOF

cat tst.r

run_RATFOR tst.r || testcase_FATAL_FAILURE
$GREP '100.*foo' stdout || testcase_FATAL_FAILURE
$GREP '200.*bar' stdout || testcase_FATAL_FAILURE

testcase_PASS

# vim: ft=sh ts=4 sw=4 et
