#!/bin/sh

# Check that ratfor accept a non-regular file, namely `/dev/null', as its
# outfile

. ../lib/rat4-testsuite-init.sh || exit 99

echo 'junk' > tst.r

run_RATFOR -o /dev/null tst.r || testcase_FAIL
test -s stderr && testcase_FAIL

testcase_DONE

# vim: ft=sh ts=4 sw=4 et
