#!/bin/sh

# Check that ratfor works on /dev/null

. ./TestsAux/defs.sh || exit 99

run_command "$RATFOR" /dev/null || testcase_FAIL

: ratfor must produce only comments on empty input
$GREP -v '^[Cc]' stdout && testcase_FAIL

testcase_PASS

# vim: ft=sh ts=4 sw=4 et
