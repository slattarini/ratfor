#!/bin/sh

# Test that `%' to output lines verbatim is recognized only if placed
# at the beginning of a line

. ./TestsAux/defs.sh || exit 99

cat >tst.r <<EOF
a = 1 %&&
b = 1; %&&
 %&&
EOF

run_command "$RATFOR" tst.r || testcase_FAIL
$FGREP "&&" stdout && testcase_FAIL

testcase_PASS

# vim: ft=sh ts=4 sw=4 et
