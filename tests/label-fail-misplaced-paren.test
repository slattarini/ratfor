#!/bin/sh

# Check that ratfor complains if a label is placed just after a left
# parenthesis, rather than bogusly complaning about `missin parenthesis'

. ../lib/rat4-t.sh || exit 99

echo '(10) foo' >tst.r

run_RATFOR -e 1 tst.r || testcase_FATAL_FAILURE
$FGREP "label following left parenthesis" stderr || testcase_FATAL_FAILURE

testcase_PASS

# vim: ft=sh ts=4 sw=4 et
