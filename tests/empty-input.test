#!/bin/sh

# Check that ratfor works on empty input

. ../lib/rat4-t.sh || exit 99

: > empty
run_RATFOR empty || testcase_FATAL_FAILURE

: ratfor must produce only comments on empty input
$GREP -v '^[Cc]' stdout && testcase_FATAL_FAILURE

testcase_PASS

# vim: ft=sh ts=4 sw=4 et
