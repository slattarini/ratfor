#!/bin/sh

# Check that ratfor comments (and only real ratfor comments) are discarded.

. ./TestsAux/test-lib.sh || exit 99

cat > tst.r <<EOF
# throw me away
"# keep me"
EOF

run_RATFOR tst.r || testcase_FAIL
$GREP -i 'throw me away' stdout && testcase_FAIL
$GREP 'keep me' stdout || testcase_FAIL

testcase_PASS

# vim: ft=sh ts=4 sw=4 et
