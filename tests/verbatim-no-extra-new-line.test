#!/bin/sh

# Check tha no extra newline is printed before an `%'-escaped line.

. ./TestsAux/defs.sh || exit 99


echo '%123' >tst.r

run_command "$RATFOR" tst.r || testcase_FAIL
$GREP "^[ $TAB]*$" stdout && testcase_FAIL

testcase_PASS

# vim: ft=sh ts=4 sw=4 et
